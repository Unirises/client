import 'package:flutter/material.dart';

class FoodDeliveryInTransitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('ride loaded, todo: implement arriving page'),
      ),
    );
  }
}
